node_modules
dist
!packages/sdk/dist/klavity-widget.iife.js
.extension
.DS_Store
.superpowers/
.env

bun.lock
sdd/

# Local Claude Code agent config (not for the repo)
.claude/

# Build artifacts
*.zip

# Prod deployment runbook — contains SSH/host access coordinates; keep OUT of the repo
docs/DEPLOYMENT.md

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
